machine:
  python:
    version:
      2.7.10

dependencies:
  override:
    - pip install -U pip
    - pip install -r requirements.txt -r test-requirements.txt

test:
  override:
    - flake8 setup.py file_metadata tests
    - python -m pytest
    - pip install .
